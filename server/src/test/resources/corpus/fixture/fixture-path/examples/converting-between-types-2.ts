const count: number = 3;
const label: string = "items in cart";
const line: string = String(count) + " " + label;

const raw: string = "42";
const total: number = Number(raw) + 8;

console.log(line);
console.log(total);
