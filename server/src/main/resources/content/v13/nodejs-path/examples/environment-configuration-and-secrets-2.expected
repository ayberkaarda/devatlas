port is a number: number 8080
database url: postgres://localhost/app
the token in a template string: [redacted]
the token through JSON.stringify: {"databaseUrl":"postgres://localhost/app","port":8080,"apiToken":"[redacted]"}
the token through util.inspect: [redacted]
the token is still readable on purpose: sk-not-a-real-token
the config object is frozen: true
writing to it throws: TypeError
loading a bad environment failed with:
configuration is invalid:
  - DATABASE_URL is required
  - PORT must be a port number, got "https://example.com"
  - API_TOKEN is required and must be at least 8 characters
