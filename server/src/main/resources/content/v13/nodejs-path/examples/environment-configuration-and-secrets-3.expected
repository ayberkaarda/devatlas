inheriting everything, the child sees: NODEJS_PATH_TOKEN,NODEJS_PATH_LOCALE
with an allow-list, the child sees: NODEJS_PATH_LOCALE
the parent still has the token: true
with --env-file, the child sees: NODEJS_PATH_LOCALE,NODEJS_PATH_FROM_FILE
the copy taken earlier: en-GB
the variable now: tr-TR
