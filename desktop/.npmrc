# npm reads .npmrc from the package directory, not from parent directories, so
# this repeats the frontend setting rather than inheriting it. See that file for
# why the engines field is enforced as an error.
engine-strict=true
